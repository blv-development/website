services:
  web:
    build: .
    volumes:
      - .:/app:delegated
      - /app/venv
    ports:
      - "8000:8000"
    environment:
      DEBUG: "1"
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
